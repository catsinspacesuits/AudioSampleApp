<h2>Samples</h2>
<% @samples.each do |sample| %>
  <ul>
    <li><h4><%= link_to sample.title, sample_path(sample) %>: </h4><p><%= sample.description %></p></li>
  </ul>
  <p><% sample.tag_list.each do |tag| %>
    <%= link_to tag, tagged_path(tag: tag) %>
  <% end %></p>
<% end %>



